# Dev-environment values for the embedding chart (M10).
# Small CPU footprint so it co-schedules with the control plane + UI on the
# single dev CPU node (m7i.large). bge-small-en-v1.5 is ~130 MB.

resources:
  requests:
    cpu: "250m"
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 2Gi
